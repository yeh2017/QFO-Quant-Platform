# 更新日志 (Changelog)

本文档记录 QFO 量化回测平台的所有版本演进与发布记录。

---

## [v1.0.2] - 2026-09-05

### 🔗 跨模块联动闭环
- **回测上下文统一打通**：在根组件引入全局 `backtestContext` 状态，打通“策略回测 → 组合优化 → 风险管理”的数据流。
- **历史回测加载联动刷新**：修复了原系统中从历史记录加载回测详情时遗漏触发组合优化刷新的缺陷。现在加载历史记录，组合优化与风险度量均由父组件统一自动重算。
- **面板来源透明展示**：在“组合优化”与“风险管理”面板顶部增加回测来源标签（如：`📎 因子选股 · 2024-01-02 ~ 2026-07-13 · 自选池 5 只`），消除用户切换 Tab 后的来源困惑。
- **非阻塞式优雅告警反馈**：当风险分析或组合优化因标的不足计算失败时，采用追加警告提示（warning alert）明确告知原因，不再静默吞错，且不阻塞主回测结果。

### 🚀 一键启动器重大优化（开箱即用保障）
- **多镜像源全自动容灾降级**：`run_first_time.bat` 彻底废弃单一清华源硬编码（根治因校园网/企业网 DNS 无法解析导致的 `[Errno 11001] getaddrinfo failed` 及 403 阻断），升级为三级自动降级机制：**阿里云（国内高速 Anycast CDN 首选） → 腾讯云（镜像重试） → 官方 PyPI（全网兜底）**。
- **前端 npm 镜像自动容灾**：官方 npm registry 失败时自动尝试 `npmmirror`（淘宝/阿里源）。
- **Windows 批处理变量冻结 Bug 修复**：根治了 Windows CMD 在括号复合块 `(...)` 中 `%errorlevel%` 预解析冻结为常量的原生暗坑，全面采用动态求值 `if errorlevel 1`，避免安装成功后被误判退出。
- **路径含空格安全处理**：服务启动切换为 `pushd` / `popd` 目录栈机制，避免项目放置在桌面或含空格目录时启动中断。

### 🧹 代码健康度与环境兼容
- **依赖审计修复**：清理了 `backend/utils/ssl_fix.py` 中未在 `requirements.txt` 声明且从未生效的冗余 `httpx` 备用导入，严格遵守 KISS 原则。
- **FastAPI 0.141+ 兼容**：优化路由反射机制，全面兼容新版 Starlette/FastAPI 的内部结构。
- **自动化测试 100% 通过**：通过 69 项前端单测（Vitest）、66 项后端单测（Pytest）以及 34 项平台架构质量审计规范。

---

## [v1.0.1] - 2026-08-15

- 完善数据中台（Header 三卡片支持实时资产统计与状态查询）。
- 支持 ETF 与股票双源智能筛选、行业轮动热力图与个股关联反查。
- 增强策略回测引擎（支持均线择时、MACD动量、双低转债、因子选股等多维度策略）。
- 引入新闻舆情与情感分析管道（支持规则评分与 LLM 智能打分）。

---

## [v1.0.0] - 2026-07-20

- 初始发布：免费开源、本地运行的 A 股量化研究与回测平台。
- 支持 Windows 一键脚本启动（`run_first_time.bat` / `run_quick_start.bat`）。
